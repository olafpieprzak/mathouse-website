{
  "name": "mathouse-tidycal-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker — proxy do TidyCal API dla MATHOUSE",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:tidycal": "wrangler secret put TIDYCAL_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
